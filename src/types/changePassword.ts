export type ChangePassword = {
     currentPassword: string;
     newPassword: string;
     confirmPassword: string;
}

export type joinUser  = {
     password: string;
     confirmPassword: string;
}