enum Role {
     OWNER = 'Owner',
     ADMIN = 'Admin',
     VIEWER = 'Viewer'
}

enum Status {
     ACTIVE = 'Active',
     DEACTIVE = 'Deactive'
}

enum Plan {
     FREE = 'Free',
     BASIC = 'Basic',
     PRO = 'Pro'
}

export {
     Role,
     Status,
     Plan,
}